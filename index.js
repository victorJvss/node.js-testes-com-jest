/* eslint-disable no-console */
const salarioTotal = (salario, horasExtra) => salario + horasExtra;
const descontoSalario = (salario, desconto) => salario - desconto;

export { salarioTotal, descontoSalario };
